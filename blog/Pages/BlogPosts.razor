@inherits Blog.Pages.BlogPosts2
@page "/blogposts"

<h1>Blogposts</h1>

@if (blogposts == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Title</th>
                <th>Date</th>
                <th>Url</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var blogpost in blogposts)
            {
                <tr>
                    <td>@blogpost.DisplayName</td>
                    <td>@blogpost.Date</td>
                    <td><a href="/blog/@GetIndex(@blogpost)">@blogpost.DisplayName</a></td>
                </tr>
            }
        </tbody>
    </table>
}